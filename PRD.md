# T-Rex Runner Clone — PRD

**Version:** 2.0
**Date:** 2026-05-08
**Owner:** 1인 개발
**Status:** Draft

---

## 1. 제품 개요

Chrome 브라우저의 오프라인 공룡 게임(인터넷 연결이 끊겼을 때 표시되는 숨겨진 게임)을 웹 기반으로 복제한다. HTML5 Canvas + Vanilla JavaScript로 구현하며, 외부 의존성을 최소화하여 단일 HTML 파일에 가까운 형태로 배포 가능하게 한다.

원본과의 가장 큰 차이는 **글로벌 리더보드**를 핵심 동기부여 장치로 추가한다는 점이다. Endless runner 장르에서 재방문률을 결정하는 가장 강력한 변수는 친구·낯선 사람과의 경쟁이며, 단일 사용자 베스트 기록만으로는 한 번 플레이 후 이탈하기 쉽다.

학습 목표는 게임 루프, 충돌 감지, 스프라이트 애니메이션의 기초와 함께 Firebase를 활용한 실시간 데이터 동기화·서버 측 검증의 기초를 습득하는 것이다.

---

## 2. 핵심 목표

| 항목 | 목표값 |
|---|---|
| 목표 FPS | 60 |
| 번들 크기 | < 200KB (Firebase SDK 트리쉐이킹 후) |
| 개발 기간 | 3주 |
| 월 운영비 | $0 (Firebase Spark Plan 무료 티어 내) |

Firebase Spark Plan은 일 5만 reads, 2만 writes까지 무료이며, 이는 일간 활성 사용자(DAU) 약 1,000명 수준까지 무과금으로 운영 가능한 수준이다.

---

## 3. 기능 요구사항

### F-01. 공룡 캐릭터 조작
- 스페이스바 또는 ↑로 점프
- ↓로 숙이기 (지상 + 공중)
- 점프 중 ↓ 입력 시 빠른 하강 (variable jump height)
- 입력 즉시 반응, 입력 지연 없음

### F-02. 장애물 & 난이도 시스템
- **선인장**: 소(12×24), 중(18×30), 대(24×36) 3종 — 지상 장애물
- **익룡(pterodactyl)**: 30×18 사이즈, 3가지 고도(낮음/중간/높음)로 등장 — 공중 장애물
- **스폰 로직**: 무작위 간격, 최소 거리 보장
- **난이도 곡선**: 점수 기반으로 스크롤 속도 점진 증가, 700점 이후 익룡 등장

### F-03. 충돌 감지 & 게임 오버
- AABB(Axis-Aligned Bounding Box) 박스 충돌
- 충돌 시 게임 즉시 정지
- "GAME OVER" 표시 후 점수 등록 흐름 진입
- 스페이스바로 재시작

### F-04. 로컬 점수 & 최고 기록
- 시간 기반 점수 누적 (프레임당 약 0.1점)
- localStorage에 개인 최고 기록 저장
- 화면 우상단에 현재 점수 / HI 점수 표기
- 오프라인에서도 개인 베스트 추적 가능

### F-05. 글로벌 리더보드 ★ 핵심 차별점
- Firebase Firestore 기반 실시간 리더보드
- Top 10 표시
- 점수 등록, 닉네임 검증, 서버 측 부정행위 방지
- 본인 순위가 10위 밖이면 하단에 별도 표기 ("내 순위: 47 / 12,341")
- 24시간 이내 신규 기록은 `NEW` 배지

### F-06. 낮/밤 전환
- 700점마다 색상 반전
- 밤 모드에서 별, 달 추가 렌더링
- 점진적 전환 애니메이션

---

## 4. 리더보드 시스템 상세

### 4.1 데이터 모델 (Firestore)


**인덱스:**
- `score` 내림차순 (Top N 조회용)
- `createdAt` 내림차순 (NEW 배지 판정용)

### 4.2 부정행위 방지 전략

클라이언트 게임은 본질적으로 신뢰할 수 없다. 점수를 만들어내는 로직이 사용자 브라우저에서 돌아가는 이상, 어떤 검증도 100% 우회 불가능하지 않다. 따라서 완벽한 방지가 아니라 **합리적 의심 필터**를 적용한다 — 정상 플레이어는 통과시키고, 명백히 비정상적인 등록만 거부한다.

| 검증 | 위치 | 로직 |
|---|---|---|
| 속도 검증 | Cloud Function | `score / duration`이 이론 최대치(약 25 점/초) 초과 시 거부 |
| 입력 비율 | Cloud Function | 입력 횟수가 비현실적으로 적으면 거부 (예: 1000점에 5회 미만) |
| 레이트 리밋 | Cloud Function | 동일 IP에서 분당 5회 이상 등록 차단 |
| 상한선 | Cloud Function | 99,999점 초과 등록 거부 |
| 권한 제어 | Firestore Rules | 본인 점수만 쓰기 가능, 모두 읽기 가능 |
| 닉네임 검증 | Cloud Function | 길이 3-12자, 비속어 사전 매칭 |

### 4.3 등록 흐름

1. 게임 오버 발생
2. 현재 점수 ≥ 리더보드 Top 10 최저점 확인
3. 자격 있으면 닉네임 입력 UI 노출
4. 클라이언트 측 닉네임 검증 (길이 + 기본 비속어 필터)
5. Cloud Function 호출 → 서버 검증 → Firestore 쓰기
6. 리더보드 자동 갱신 (Firestore 실시간 리스너)

### 4.4 표시 규칙

- 기본 Top 10 표시
- 본인 순위가 10위 밖이면 하단에 "내 순위: N / 전체" 별도 표기
- 동점은 먼저 등록된 사람이 상위
- 24시간 이내 신규 기록은 `NEW` 배지
- 1위는 강조 색상(금색 계열), 2-3위는 보조 강조

---

## 5. 게임 파라미터 (튜닝 기준)

| 파라미터 | 초기값 | 설명 |
|---|---|---|
| 초기 스크롤 속도 | 6 px/frame | 게임 시작 시 배경 이동 속도 |
| 최대 스크롤 속도 | 12 px/frame | 점수 증가에 따른 상한 |
| 중력 | 0.6 | 점프 후 낙하 가속도 |
| 점프 초기 속도 | -12 | 점프 시 수직 속도 (음수 = 위) |
| 장애물 스폰 간격 | 80 frames | 평균 스폰 주기 (난수 적용) |
| 익룡 등장 점수 | 700 | 이 점수부터 공중 장애물 등장 |
| 낮/밤 전환 점수 | 700 | 이 주기마다 색상 반전 |

이 값들은 60Hz 기준이며, 다른 주사율에서도 같은 게임 느낌을 주기 위해 delta time 기반 정규화가 필요하다.

---

## 6. 기술 스택

| 영역 | 기술 |
|---|---|
| 렌더링 | HTML5 Canvas 2D API |
| 언어 | Vanilla JavaScript (ES2020+) |
| 게임 루프 | `requestAnimationFrame` |
| 로컬 저장 | localStorage |
| 데이터베이스 | Firebase Firestore |
| 서버 검증 | Firebase Cloud Functions (Node.js) |
| 호스팅 | Firebase Hosting |
| 인증 | Firebase Anonymous Auth |
| 에셋 | 스프라이트 시트 1장 (PNG) |
| 빌드 | esbuild 또는 Vite |

---

## 7. 개발 단계 (3주)

### Week 1 — 게임 코어
- Day 1-2: Canvas 셋업, 게임 루프, 공룡 점프 물리
- Day 3-5: 스크롤 배경, 장애물 스폰 시스템
- Day 6-7: 충돌 감지, 게임 오버, 재시작 흐름

### Week 2 — 게임 폴리싱
- Day 8-9: 스프라이트 애니메이션, 익룡 추가, 숙이기
- Day 10-12: 점수 시스템, 최고 기록, 낮/밤 전환
- Day 13-14: 사운드, 모바일 터치 대응, 폴리싱

### Week 3 — 리더보드 ★
- Day 15-16: Firebase 프로젝트 셋업, Firestore 스키마, Anonymous Auth
- Day 17-18: 리더보드 UI, 실시간 리스너 연동
- Day 19-20: Cloud Function 작성 (서버 검증, 부정행위 필터)
- Day 21: Security Rules 작성, 통합 테스트, 배포

---

## 8. 비기능 요구사항

### 성능
- 60 FPS 유지 (일반 데스크톱 브라우저 기준)
- 게임 시작까지 첫 페이지 로드 1초 이내 (3G 환경)
- 리더보드 조회 응답 시간 500ms 이내

### 호환성
- Chrome, Firefox, Safari, Edge 최신 2개 메이저 버전
- 모바일 Chrome / Safari (터치 입력 지원)
- 키보드 + 터치 + 마우스 클릭 모두 점프로 처리

### 접근성
- 키보드만으로 모든 기능 조작 가능
- 색약 사용자 고려 (명도 대비 충분히 확보)
- ARIA 라벨로 상태 변화 안내

---

## 9. 향후 확장 (Out of MVP)

- 캐릭터 스킨 시스템: 점수 기반 언락 또는 단순 선택지
- 일일 챌린지: 매일 리셋되는 별도 리더보드, 특정 조건 모드
- 친구 시스템: 초대 링크 기반 비공식 그룹 리더보드
- 국가별/지역별 리더보드 분리
- 모바일 PWA 패키징: 오프라인 설치 가능
- 리플레이 저장 & 공유: 입력 시퀀스만 저장하여 결정론적 리플레이
- 멀티플레이어 동시 달리기: WebSocket 기반 실시간 대결

---

## 10. 위험 요소 & 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| 부정행위 점수 등록 | 리더보드 신뢰도 하락 | Cloud Function 다층 검증, 의심 점수 수동 검토 큐 |
| Firebase 무료 티어 초과 | 서비스 중단 | 리더보드 캐싱(5초 단위), 알림 설정, Blaze Plan 백업 |
| 모바일 입력 지연 | 게임플레이 품질 저하 | `touchstart` 이벤트 우선, `passive: false` 처리 |
| 다양한 주사율 | 게임 속도 불균일 | delta time 기반 움직임 계산 |
| 비속어/혐오 닉네임 | 사용자 경험 악화 | 비속어 사전 + 신고 버튼 + 24시간 내 수동 검토 |

---

## 11. 성공 지표 (출시 후 30일)

- 게임 완성도: 크리티컬 버그 0건
- 참여: 1인당 평균 플레이 횟수 5회 이상
- 리더보드 활성도: 일일 신규 점수 등록 50건 이상
- 재방문: 7일 재방문률 20% 이상
- 부정행위: 의심 점수 비율 5% 이하

---

## 부록 A. 참고 자료

- Chrome 원본 소스: `chrome://dino` (오프라인 시 자동 표시)
- 원본 코드 위치: Chromium 프로젝트 내 `components/neterror/resources/offline.js`
- Firebase Firestore 문서: https://firebase.google.com/docs/firestore
- Firebase Spark Plan 한도: https://firebase.google.com/pricing