{
  "name": "yjb-3",
  "private": true,
  "version": "1.0.0",
  "description": "T-Rex Runner clone with global leaderboard (Supabase + Vercel).",
  "type": "module",
  "engines": { "node": "20.x" },
  "scripts": {
    "test": "node tests/logic.test.mjs",
    "deploy": "vercel deploy --prod --yes"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  }
}
